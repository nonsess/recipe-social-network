# Server settings
SERVER__URL=http://localhost
SERVER__HOST=0.0.0.0
SERVER__PORT=8000
SERVER__ALLOWED_ORIGINS=["http://localhost:3000"]

# PostgreSQL settings
POSTGRES__USER=postgres
POSTGRES__PASSWORD=postgres
POSTGRES__HOST=localhost
POSTGRES__PORT=5432
POSTGRES__DB=food_social_network
POSTGRES__ECHO=false

# S3 settings
S3_STORAGE__HOST=http://localhost
S3_STORAGE__PORT=9000
S3_STORAGE__ACCESS_KEY=your-access-key
S3_STORAGE__SECRET_KEY=your-secret-key

# JWT settings
JWT__SECRET_KEY=your-secret-key-here
JWT__ALGORITHM=HS256
JWT__ACCESS_TOKEN_EXPIRE_MINUTES=30
JWT__REFRESH_TOKEN_EXPIRE_DAYS=7

# Redis settings
REDIS__HOST=localhost
REDIS__PORT=6379

# Application settings
MODE=dev
PROJECT_NAME=Food Social Network
